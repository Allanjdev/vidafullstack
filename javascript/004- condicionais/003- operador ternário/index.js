// let salario = 1000;
// let bonus = 0.0;

// if (salario > 1000) {
//   bonus = salario * 0.1;
// } else {
//   bonus = salario * 0.15;
// }
// console.log(bonus);

// Operador ternário

// let salario = 1000;
// let bonus = salario * (salario > 1000 ? 0.1 : 0.15);
// console.log(bonus);

const velocidade = 75;
const warn = 85;

const condicao =
  velocidade >= warn ? console.log("Multado") : console.log("prossiga viagem");
// console.log(condicao); // não imprime nada pois é uma função que não retorna nada
